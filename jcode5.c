#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <math.h>

int main()
{
	int t,r,p;
	scanf("%d",&t);
	while(t--)
	{
		scanf("%d",p);
		char ar1[p+3],ar2[p+3];
		scanf("%s",ar1);
		for(i=0;i<strlen(ar1);i++)
			
		scanf("%s",ar2);
		
		r=pow(2,m);
		printf("%d",r);
	}
	return 0;
}
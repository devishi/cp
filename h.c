	#include <stdio.h>
	int main()
	{
		int i=0;
		char z='a',arr[1000];
		while(z!='\n')
			{
				z=arr[i];
			scanf("%c",&arr[i]);
			z=arr[i];
			i++;
			}
			printf("Hacker rank %s",arr );
		return 0;
	}